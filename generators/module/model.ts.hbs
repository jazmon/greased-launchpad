import { default as knex } from 'db';
import { BaseModel, Maybe } from 'types';
import { {{pascalCase name}} } from './type';

export class {{pascalCase namePlural}} {
  async getAll(): Promise<{{pascalCase name}}[]> {
    return knex('{{snakeCase namePlural}}');
  }
  async getById(id: string): Promise<Maybe<{{pascalCase name}}>> {
    const query = knex('{{snakeCase namePlural}}').where({ id });
    return query.then(([row]) => row);
  }
}
